{{ form_start(form) }}
<section class="">
	<div class="container">
		<div class="row text-center">
			<h2 class="my-4">Modifier mon profil</h2>
			<div class="col-6">
				{{ form_row(form.firstName, { 'name': 'Prénom' }) }}
			</div>
			<div class="col-6">
				{{ form_row(form.lastName, { 'name': 'Nom' }) }}
			</div>
			<div class="col-12">
				{{ form_row(form.email) }}
			</div>
			<div class="col-12">
				{{ form_row(form.plainPassword, { 'name': 'Mot de passe' }) }}
			</div>
			<div class="col-12">
				{{ form_row(form.subscription, { 'name': 'Abonnement' }) }}
			</div>
			<div class="col-6">
				{{ form_row(form.send, {'name': 'Modifier'}) }}
			</div>
			<div class="col-6">
				{{ include('user/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
</section>
{{ form_end(form) }}
